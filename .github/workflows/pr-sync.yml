name: PR

on:
  pull_request_target:
    types:
      - opened
      - edited
      - reopened
      - synchronize

jobs:
  sync:
    uses: ./.github/workflows/_pr-sync.yml
    permissions:
      pull-requests: write
      issues: write
